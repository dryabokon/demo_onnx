[project]
name = "onnx-demo"
version = "0.1.0"
description = "YOLO based onnx test"
requires-python = ">=3.10"

dependencies = [
    "ultralytics",
    "onnxruntime",
    "pandas",
    "numpy",
    "opencv-python",
    "scikit-image",
    "tabulate"
]

